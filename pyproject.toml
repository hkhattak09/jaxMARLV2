[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jaxmarl"
version = "0.1.0"
description = "Stripped down JaxMARL for SMAX CTM"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["jaxmarl*"]
